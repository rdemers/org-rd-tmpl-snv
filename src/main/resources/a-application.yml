#
spring:
  h2:
    console:
      enabled: true
      path: /h2-ui
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        ddl-auto: update
#
# App Properties
com:
  lq:
    sdj:
      pdc:
        secjwt:
          secret: la.belle.cle.secrete
          expiration: 30000
          authorities: lq.roles
#